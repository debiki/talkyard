import * as path from 'path';
import * as webpack from 'webpack';

module.exports = {
  mode: 'production',
  entry: {
    'talkyard-service-worker': './client/serviceworker/service-worker.ts',
  },
  module: {
    rules: [{
      test: /\.tsx?$/,
      use: 'ts-loader',
      exclude: /node_modules/,
    }],
  },
  resolve: {
    extensions: ['.tsx', '.ts', '.js'],
  },
  output: {
    path: path.resolve(__dirname, 'target', 'webpack'),
    filename: '[name].js'
  }
};
