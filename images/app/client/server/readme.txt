In this client/server/ directory, you'll find React.js components that are
rendered to HTML server side, by debiki-server/app/debiki/ReactRenderer.scala.

The files in here are actually softlinks to ../app/.

