

.dw-p-ttl-e
  margin-top: (30 + 15 + 8)px;  // 30 + 5 = title margin + padding, 8 = a bit extra
  margin-bottom: 38px;
  max-width: 550px;

  .dw-i-layout
    width: auto;

  .dw-save-btns-etc a
    float: none;
    margin-left: 10px;
    opacity: 0.75;
    position: relative;
    top: 3px;
    font-size: 14px;

    &.icon-settings:before
      opacity: 0.85;

  .dw-compl-stuff
    margin-top: 15px;


// vim: fdm=marker et ts=2 sw=2 tw=0 list
