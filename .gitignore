conf/prod-password.conf
scratch.txt
scratch/
logs
project/project
project/target
project/boot
dist
target
tmp
.history
.idea/
.idea_modules/
debiki-app-play.iml
RUNNING_PID

# Eclipse
.cache
.classpath
.project
.settings/

# Ignore core dump file.
core

# Ignoer shortcut to `scripts/` (autocompletion to `scripts/` doesn't work,
# because there's a /usr/bin/script program).
s

# Ignore softlinks to the Play installation directory, e.g. scripts/play-2.1.sh,
# and scripts that shouldn't be checked in.
scripts/play-2.*.sh
scripts/local

# Ignore config/local/, for config files that are not to be checked in to the
# main Git repo. (But you could place them in a Git repo, conf/local/.git,
# should you want to.)
conf/local
conf/dev-test-local.conf
conf/test-evolutions-local.conf
conf/securesocial-local.conf

# NPM packages folder.
node_modules/

# Things that shouldn't be checked in. If you want to, you can place a Git repo
# here, and softlink to its subdirectories via conf/local and scripts/local,
# and in that way have all your local things in one repo.
local

# Ignore JS and CSS bundles generated by Grunt.
public/res/combined-debiki*
public/res/debiki-spa-*
public/res/debiki-dashbar*
public/res/debiki-pagedown*
public/res/admin.css
public/themes/*

# Ignore generated test JS files.
test-client/debiki-utils.js
test-client/unit/spa/admin/*.js

# When running Chrome browser tests, ChromeDriver generates this file and it
# seems it's location is hardcoded,
#   see: http://code.google.com/p/selenium/issues/detail?id=3475
/chromedriver.log

# Ignore Vagrant's virtual machine info directory.
.vagrant/

